# .NET
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates

# Local-only config override (per-worktree ports, Mongo DB names, etc).
# Use appsettings.Local.example.json as the template.
appsettings.Local.json

# One-shot migration script backups (raw mongo dumps). Local only.
tools/backups/

# Per-machine Claude Code settings
.claude/

# Flutter/Dart
**/build/
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.idea/
.vscode/
.vs/

# OS
.DS_Store
Thumbs.db

# Misc
.env
