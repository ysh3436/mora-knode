{
  "_revisit": "Hosting.Diagnostics @ Information emits one Request started / finished pair per HTTP call — useful for tracing UI ↔ API during dev. Recommended removal point: Phase 3 public GitHub release (M3, 2026-08) so external contributors don't see verbose request lines by default. Dev-only file, no prod impact.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.AspNetCore.Hosting.Diagnostics": "Information"
    }
  },
  "Mongo": {
    "ConnectionString": "mongodb://localhost:27017",
    "Database": "mora_knode_dev"
  }
}
