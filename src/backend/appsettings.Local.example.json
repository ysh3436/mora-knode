{
  "//": "Copy this file to appsettings.Local.json and edit per-worktree.",
  "//_purpose": "Per-machine / per-worktree overrides. Gitignored. Loaded after appsettings.{Env}.json, so values here win.",
  "//_typical_use": "Running two worktrees in parallel — give each its own port + Mongo database to avoid collisions.",

  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://localhost:5163"
      }
    }
  },

  "Mongo": {
    "ConnectionString": "mongodb://localhost:27017",
    "Database": "mora_knode_dev"
  }
}
